import styles from "./styles.module.css";

const NavigationBar = () => {
  return (
    <nav className={styles.NavigationBar}>
      <ul></ul>
    </nav>
  );
};

export default NavigationBar;
